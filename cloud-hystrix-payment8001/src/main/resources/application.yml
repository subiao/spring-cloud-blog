server:
  port: 8001

spring:
  application:
    name: cloud-hsytrix-payment

eureka:
  client:
    # 注册自己
    register-with-eureka: true
    # 是否从注册中心抓取服务信息 用于负载均衡
    fetch-registry: true
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka
